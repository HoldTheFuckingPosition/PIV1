// Task 1.1 has no runtime integration tests. This file only proves that the
// pinned TypeScript workspace compiles before later test suites are added.
export const TASK_1_1_SCOPE = "compile-only" as const;
